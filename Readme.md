When you run the script (Test.py), it:

Defines a function that generates a personalized greeting.
Calls that function with "Git User" as the input.
Prints the result: Hello, Git User! Welcome to Git!